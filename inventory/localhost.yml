---
# llmkube-bootstrap only ever runs against the local machine.
# We declare localhost explicitly so ansible-lint stops complaining
# about an implicit inventory.

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
