---
# kubernetes role -- bring up the local cluster the operator runs on.
#
# Docker Desktop is installed by the homebrew role; we just need to
# make sure it is running, then create the kind cluster + verify the
# operator can talk to it.

- name: Ensure Docker Desktop is running
  ansible.builtin.command: open -ga Docker
  changed_when: false
  # `open -ga` is idempotent: silently no-ops when Docker is already up.

- name: Wait for Docker daemon to accept commands
  ansible.builtin.command: docker info
  register: docker_info
  retries: 30
  delay: 5
  until: docker_info.rc == 0
  changed_when: false

- name: List existing kind clusters
  ansible.builtin.command: kind get clusters
  register: kind_clusters
  changed_when: false
  failed_when: false

- name: Create kind cluster {{ kind_cluster_name }}
  ansible.builtin.command: >-
    kind create cluster
    --name {{ kind_cluster_name }}
    --image {{ kind_node_image }}
    --wait 5m
  when: kind_cluster_name not in (kind_clusters.stdout_lines | default([]))
  changed_when: true

- name: Set kubectl context to the kind cluster
  ansible.builtin.command: >-
    kubectl config use-context kind-{{ kind_cluster_name }}
  changed_when: false

- name: Wait for nodes to be Ready
  ansible.builtin.command: >-
    kubectl wait --for=condition=Ready nodes --all --timeout=120s
  changed_when: false
